server:
  port: 9010
spring:
  application:
    name: cloud-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8850 # Nacos ??
        username: nacos        # ?????
        password: nacos        # ????
        namespace: cloud-ll      # ???????
        group: DEFAULT_GROUP   # ?????
      # Spring Boot 3.x ?? Nacos ??????????????? bootstrap.yml?
      # ????? Nacos Config?????
    config:
      import: 'optional:nacos:'  # ??????????
    gateway:
      discovery:
        locator:
          enabled: true # ????? Nacos ????
    routes:
      - id: user-route
        uri: lb://auth-service # lb ?? LoadBalance?? Nacos ??????
        predicates:
          - Path=/auth/**